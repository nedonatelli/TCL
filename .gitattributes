# Generated HTML files for documentation examples
# These are tracked as regular git files (not LFS) to render properly on GitHub
# Regenerate with: python scripts/generate_example_plots.py
# Append-only benchmark history: concurrent CI appends must union-merge,
# not conflict (two EOF appends conflict textually under the default driver).
.benchmarks/history.jsonl merge=union
